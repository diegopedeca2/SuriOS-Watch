package com.suri.pipsurios.ui.theme

import androidx.compose.ui.graphics.Color

// Fondo principal
val PipBlack = Color(0xFF000000)

// Verde principal (texto) — EDL: #66FF66
val PipGreen = Color(0xFF66FF66)

// Verde brillante (elementos importantes)
val PipGreenBright = Color(0xFF66FF99)

// Verde tenue (texto secundario) — EDL: #3FAF5A
val PipGreenDim = Color(0xFF3FAF5A)

// Remote P.R.S. node marker
val PipBlue = Color(0xFF33AAFF)

// Gris (elementos desactivados)
val PipGray = Color(0xFF5A5A5A)

// Ámbar (avisos) — EDL: #FFC857
val PipAmber = Color(0xFFFFC857)

// Rojo (errores) — EDL: #FF4D4D
val PipRed = Color(0xFFFF4D4D)
